export const mechs = [
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "CrapBot",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "JunkBot",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "Berry",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "Clementina",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "SmartGuy",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "Konosuba",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "WhoAmI",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
{
    id: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
    name: "Brick",
    tier: function(n) { return Math.floor(Math.random() * Math.floor(n)) },
},
];
